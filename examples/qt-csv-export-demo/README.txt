QT CSV Export Demo for camt-parser

Build:
  qmake qt-csv-export-demo.pro
  make   (or mingw32-make / nmake on Windows)

Run:
  ./qt-csv-export-demo
  => generates export.csv

Make sure the library 'camt' is built in ../../build before building this demo.
